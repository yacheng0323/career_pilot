# M4b — UI 打磨 + 真實 Claude AI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 升級 Flutter App UI 質感（Skeleton loader、深色模式、Empty state、Pull-to-refresh）並接通真實 Claude API（有 key 時自動切換，結果 cache 到 SharedPreferences）。

**Architecture:** shimmer 套件提供骨架屏動畫；AppTheme 加 dark mode；AiService 改為 apiKey 非空時強制使用真實 API（不再依賴 kDebugMode）；分析結果以 jobId 為 key cache 到 SharedPreferences，避免重複呼叫。

**Tech Stack:** Flutter, shimmer ^3.0.0, Riverpod, SharedPreferences（現有）, Claude API（現有 http client）

**Branch:** `feature/m4b-ui-ai`

---

## File Structure

```
lib/
├── core/
│   ├── ai/
│   │   └── ai_service.dart          # MODIFY — key 非空即用真實 API；加 retry + cache
│   └── theme/
│       └── app_theme.dart           # MODIFY — 加 dark ThemeData
├── app/
│   └── app.dart                     # MODIFY — themeMode: ThemeMode.system
├── features/jobs/presentation/
│   ├── widgets/
│   │   ├── job_card.dart            # (unchanged)
│   │   └── job_card_skeleton.dart   # NEW — shimmer skeleton card
│   └── screens/
│       ├── job_list_screen.dart     # MODIFY — skeleton loading + pull-to-refresh + better empty state
│       └── job_detail_screen.dart   # (unchanged — AI card already exists)
pubspec.yaml                          # MODIFY — add shimmer ^3.0.0
test/
└── m4b_test.dart                    # NEW — AiService cache + dark theme tests
```

---

## Task 1: 加入 shimmer 依賴

**Files:** `pubspec.yaml`

- [ ] **Step 1: 編輯 pubspec.yaml，在 `http: ^1.2.2` 後加入**

```yaml
  shimmer: ^3.0.0
```

- [ ] **Step 2: flutter pub get**

```powershell
$env:PATH = "D:\flutter\bin;$env:PATH"
cd C:\dev\career_pilot
flutter pub get 2>&1 | Select-Object -Last 3
```

Expected: `Got dependencies!`

- [ ] **Step 3: dart analyze 確認無錯**

```powershell
dart analyze lib/ 2>&1
```

Expected: `No issues found!`

- [ ] **Step 4: Commit**

```powershell
git add pubspec.yaml pubspec.lock
git commit -m "chore(m4b): add shimmer dependency"
```

---

## Task 2: 深色模式 AppTheme + App

**Files:**
- Modify: `lib/core/theme/app_theme.dart`
- Modify: `lib/app/app.dart`

- [ ] **Step 1: 寫失敗測試**

建立 `C:\dev\career_pilot\test\m4b_test.dart`：

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:career_pilot/core/theme/app_theme.dart';

void main() {
  group('AppTheme', () {
    test('light theme uses Material3', () {
      expect(AppTheme.light.useMaterial3, isTrue);
    });

    test('dark theme exists and uses Material3', () {
      expect(AppTheme.dark.useMaterial3, isTrue);
    });

    test('dark theme brightness is dark', () {
      expect(
        AppTheme.dark.colorScheme.brightness,
        Brightness.dark,
      );
    });

    test('light and dark use same seed color', () {
      final lightPrimary = AppTheme.light.colorScheme.primary;
      final darkPrimary = AppTheme.dark.colorScheme.primary;
      // Both derived from same seed — primaries should differ but be non-null
      expect(lightPrimary, isNotNull);
      expect(darkPrimary, isNotNull);
    });
  });
}
```

- [ ] **Step 2: 執行確認失敗**

```powershell
$env:PATH = "D:\flutter\bin;$env:PATH"
cd C:\dev\career_pilot
flutter test test/m4b_test.dart 2>&1 | Select-Object -Last 5
```

Expected: compile error（`AppTheme.dark` 不存在）

- [ ] **Step 3: 更新 app_theme.dart**

覆寫 `C:\dev\career_pilot\lib\core\theme\app_theme.dart`：

```dart
import 'package:flutter/material.dart';

const _seedColor = Color(0xFF1A73E8);

class AppTheme {
  AppTheme._();

  static ThemeData get light => ThemeData(
        useMaterial3: true,
        colorScheme: ColorScheme.fromSeed(
          seedColor: _seedColor,
          brightness: Brightness.light,
        ),
        cardTheme: CardThemeData(
          elevation: 2,
          shape: RoundedRectangleBorder(
            borderRadius: BorderRadius.circular(12),
          ),
        ),
        inputDecorationTheme: InputDecorationTheme(
          filled: true,
          border: OutlineInputBorder(
            borderRadius: BorderRadius.circular(12),
            borderSide: BorderSide.none,
          ),
          contentPadding: const EdgeInsets.symmetric(
            horizontal: 16,
            vertical: 12,
          ),
        ),
      );

  static ThemeData get dark => ThemeData(
        useMaterial3: true,
        colorScheme: ColorScheme.fromSeed(
          seedColor: _seedColor,
          brightness: Brightness.dark,
        ),
        cardTheme: CardThemeData(
          elevation: 2,
          shape: RoundedRectangleBorder(
            borderRadius: BorderRadius.circular(12),
          ),
        ),
        inputDecorationTheme: InputDecorationTheme(
          filled: true,
          border: OutlineInputBorder(
            borderRadius: BorderRadius.circular(12),
            borderSide: BorderSide.none,
          ),
          contentPadding: const EdgeInsets.symmetric(
            horizontal: 16,
            vertical: 12,
          ),
        ),
      );
}
```

- [ ] **Step 4: 更新 app.dart 加入深色模式**

覆寫 `C:\dev\career_pilot\lib\app\app.dart`：

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import '../core/theme/app_theme.dart';
import 'router.dart';

class App extends StatelessWidget {
  const App({super.key});

  @override
  Widget build(BuildContext context) {
    return ProviderScope(
      child: MaterialApp.router(
        title: 'Career Pilot',
        theme: AppTheme.light,
        darkTheme: AppTheme.dark,
        themeMode: ThemeMode.system,   // follows device setting
        routerConfig: appRouter,
        debugShowCheckedModeBanner: false,
      ),
    );
  }
}
```

- [ ] **Step 5: 執行測試確認通過**

```powershell
flutter test test/m4b_test.dart 2>&1 | Select-Object -Last 5
```

Expected: `+4: All tests passed!`

- [ ] **Step 6: 執行全部測試**

```powershell
flutter test 2>&1 | Select-Object -Last 5
```

Expected: All passed（37 舊 + 4 新 = 41）

- [ ] **Step 7: Commit**

```powershell
git add lib/core/theme/app_theme.dart lib/app/app.dart test/m4b_test.dart
git commit -m "feat(m4b): add dark mode theme and system theme switching"
```

---

## Task 3: JobCard Skeleton Widget

**Files:**
- Create: `lib/features/jobs/presentation/widgets/job_card_skeleton.dart`

- [ ] **Step 1: 建立 skeleton widget**

建立 `C:\dev\career_pilot\lib\features\jobs\presentation\widgets\job_card_skeleton.dart`：

```dart
import 'package:flutter/material.dart';
import 'package:shimmer/shimmer.dart';

/// Placeholder card shown while job list is loading.
/// Matches the visual height of a real JobCard.
class JobCardSkeleton extends StatelessWidget {
  const JobCardSkeleton({super.key});

  @override
  Widget build(BuildContext context) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    final baseColor = isDark ? Colors.grey[800]! : Colors.grey[300]!;
    final highlightColor = isDark ? Colors.grey[700]! : Colors.grey[100]!;

    return Shimmer.fromColors(
      baseColor: baseColor,
      highlightColor: highlightColor,
      child: Card(
        margin: const EdgeInsets.symmetric(horizontal: 16, vertical: 8),
        child: Padding(
          padding: const EdgeInsets.all(16),
          child: Column(
            crossAxisAlignment: CrossAxisAlignment.start,
            children: [
              // Title row
              Row(
                children: [
                  Expanded(
                    child: _Box(height: 16, width: double.infinity),
                  ),
                  const SizedBox(width: 12),
                  _Box(height: 16, width: 60),
                ],
              ),
              const SizedBox(height: 10),
              // Company
              _Box(height: 12, width: 140),
              const SizedBox(height: 10),
              // Location + salary row
              Row(
                children: [
                  _Box(height: 12, width: 80),
                  const SizedBox(width: 16),
                  _Box(height: 12, width: 100),
                ],
              ),
              const SizedBox(height: 12),
              // Skill chips row
              Row(
                children: [
                  _Box(height: 24, width: 60, radius: 12),
                  const SizedBox(width: 8),
                  _Box(height: 24, width: 50, radius: 12),
                  const SizedBox(width: 8),
                  _Box(height: 24, width: 70, radius: 12),
                ],
              ),
            ],
          ),
        ),
      ),
    );
  }
}

class _Box extends StatelessWidget {
  const _Box({required this.height, required this.width, this.radius = 6});

  final double height;
  final double width;
  final double radius;

  @override
  Widget build(BuildContext context) {
    return Container(
      height: height,
      width: width,
      decoration: BoxDecoration(
        color: Colors.white,
        borderRadius: BorderRadius.circular(radius),
      ),
    );
  }
}
```

- [ ] **Step 2: dart analyze**

```powershell
$env:PATH = "D:\flutter\bin;$env:PATH"
cd C:\dev\career_pilot
dart analyze lib/features/jobs/presentation/widgets/job_card_skeleton.dart 2>&1
```

Expected: `No issues found!`

- [ ] **Step 3: Commit**

```powershell
git add lib/features/jobs/presentation/widgets/job_card_skeleton.dart
git commit -m "feat(m4b): add JobCardSkeleton shimmer widget"
```

---

## Task 4: JobListScreen — Skeleton Loading + Empty State + Pull-to-refresh

**Files:**
- Modify: `lib/features/jobs/presentation/screens/job_list_screen.dart`

- [ ] **Step 1: 讀取現有 job_list_screen.dart 確認結構**

讀取 `C:\dev\career_pilot\lib\features\jobs\presentation\screens\job_list_screen.dart`，找到：
1. `loading:` handler（目前是 `CircularProgressIndicator`）
2. `filtered.isEmpty` empty state block
3. `ListView.builder` 的 itemBuilder

- [ ] **Step 2: 修改 loading handler — 換成 skeleton list**

找到這段：
```dart
              loading: () =>
                  const Center(child: CircularProgressIndicator()),
```

替換為：
```dart
              loading: () => _SkeletonList(),
```

- [ ] **Step 3: 在檔案頂端加入 skeleton import**

在 `job_list_screen.dart` 的 import 區加入：
```dart
import '../widgets/job_card_skeleton.dart';
```

- [ ] **Step 4: 改善 empty state**

找到這段 empty state：
```dart
                if (filtered.isEmpty) {
                  return Center(
                    child: Column(
                      mainAxisSize: MainAxisSize.min,
                      children: [
                        Icon(Icons.search_off,
                            size: 48, color: colors.onSurfaceVariant),
                        const SizedBox(height: 12),
                        const Text('找不到符合的職缺'),
                        if (_hasActiveFilter) ...[
                          const SizedBox(height: 8),
                          TextButton(
                              onPressed: _clearAllFilters,
                              child: const Text('清除篩選')),
                        ],
                      ],
                    ),
                  );
                }
```

替換為：
```dart
                if (filtered.isEmpty) {
                  return _EmptyState(
                    hasFilter: _hasActiveFilter,
                    onClearFilter: _clearAllFilters,
                    onSync: () => ref.read(syncNotifierProvider.notifier).sync(),
                  );
                }
```

- [ ] **Step 5: 加入 Pull-to-refresh**

找到 `ListView.builder(` 那段，用 `RefreshIndicator` 包起來：

```dart
                return RefreshIndicator(
                  onRefresh: () async {
                    ref.invalidate(jobListProvider);
                    await ref.read(jobListProvider(query: _query).future);
                  },
                  child: ListView.builder(
                    itemCount: filtered.length,
                    padding: const EdgeInsets.only(bottom: 16),
                    itemBuilder: (_, i) => JobCard(job: filtered[i]),
                  ),
                );
```

- [ ] **Step 6: 在檔案最末尾加入 _SkeletonList 和 _EmptyState widget**

```dart
class _SkeletonList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ListView.builder(
      physics: const NeverScrollableScrollPhysics(),
      itemCount: 6,
      padding: const EdgeInsets.only(bottom: 16),
      itemBuilder: (_, __) => const JobCardSkeleton(),
    );
  }
}

class _EmptyState extends StatelessWidget {
  const _EmptyState({
    required this.hasFilter,
    required this.onClearFilter,
    required this.onSync,
  });

  final bool hasFilter;
  final VoidCallback onClearFilter;
  final VoidCallback onSync;

  @override
  Widget build(BuildContext context) {
    final colors = Theme.of(context).colorScheme;
    final textTheme = Theme.of(context).textTheme;

    return Center(
      child: Padding(
        padding: const EdgeInsets.all(32),
        child: Column(
          mainAxisSize: MainAxisSize.min,
          children: [
            Icon(
              hasFilter ? Icons.filter_list_off : Icons.work_outline,
              size: 72,
              color: colors.onSurfaceVariant.withValues(alpha: 0.4),
            ),
            const SizedBox(height: 16),
            Text(
              hasFilter ? '找不到符合的職缺' : '還沒有職缺資料',
              style: textTheme.titleMedium?.copyWith(
                color: colors.onSurfaceVariant,
                fontWeight: FontWeight.bold,
              ),
            ),
            const SizedBox(height: 8),
            Text(
              hasFilter
                  ? '試試調整篩選條件'
                  : '點擊右上角 ↻ 按鈕同步最新職缺',
              style: textTheme.bodyMedium?.copyWith(
                color: colors.onSurfaceVariant.withValues(alpha: 0.7),
              ),
              textAlign: TextAlign.center,
            ),
            const SizedBox(height: 24),
            if (hasFilter)
              FilledButton.tonal(
                onPressed: onClearFilter,
                child: const Text('清除篩選'),
              )
            else
              FilledButton.icon(
                onPressed: onSync,
                icon: const Icon(Icons.sync),
                label: const Text('立即同步'),
              ),
          ],
        ),
      ),
    );
  }
}
```

- [ ] **Step 7: dart analyze**

```powershell
dart analyze lib/ 2>&1
```

Fix any issues. Expected: `No issues found!`

- [ ] **Step 8: flutter test**

```powershell
flutter test 2>&1 | Select-Object -Last 5
```

Expected: All passed（41 tests）

- [ ] **Step 9: Commit**

```powershell
git add lib/features/jobs/presentation/screens/job_list_screen.dart lib/features/jobs/presentation/widgets/job_card_skeleton.dart
git commit -m "feat(m4b): skeleton loading, improved empty state, pull-to-refresh"
```

---

## Task 5: AiService — 真實 API + Retry + Cache

**Files:**
- Modify: `lib/core/ai/ai_service.dart`

- [ ] **Step 1: 寫失敗測試（加入 m4b_test.dart）**

在 `C:\dev\career_pilot\test\m4b_test.dart` 的 `main()` 末尾補充：

```dart
  group('AiService', () {
    test('uses mock when apiKey is empty', () {
      final svc = AiService(apiKey: '');
      expect(svc.useMock, isTrue);
    });

    test('uses real API when apiKey is provided', () {
      final svc = AiService(apiKey: 'sk-ant-test');
      expect(svc.useMock, isFalse);
    });

    test('mock returns 3 non-empty bullets', () async {
      final svc = AiService(apiKey: '', useMock: true);
      final result = await svc.analyze(
        jobTitle: 'Flutter Dev',
        company: 'Acme',
        description: 'Build apps',
        jobSkills: ['Flutter', 'Dart'],
        userSkills: ['Flutter'],
      );
      expect(result.summaryBullets.length, 3);
      expect(result.summaryBullets.every((b) => b.isNotEmpty), isTrue);
    });

    test('match score 0-100', () async {
      final svc = AiService(apiKey: '', useMock: true);
      final result = await svc.analyze(
        jobTitle: 'Flutter Dev',
        company: 'Acme',
        description: 'Build apps',
        jobSkills: ['Flutter', 'Dart'],
        userSkills: [],
      );
      expect(result.matchScore, inInclusiveRange(0, 100));
    });
  });
```

- [ ] **Step 2: 執行確認新測試失敗**

```powershell
$env:PATH = "D:\flutter\bin;$env:PATH"
cd C:\dev\career_pilot
flutter test test/m4b_test.dart 2>&1 | Select-Object -Last 8
```

Expected: 第 2 個 test 失敗（`uses real API when apiKey is provided` — 目前 key 非空還是看 kDebugMode）

- [ ] **Step 3: 更新 AiService — key 非空即用真實 API**

讀取現有 `lib/core/ai/ai_service.dart`，修改建構子邏輯：

找到這行：
```dart
      : useMock = useMock ?? kDebugMode;
```

替換為：
```dart
      : useMock = useMock ?? apiKey.isEmpty;
```

說明：`apiKey` 為空（未傳 `--dart-define=CLAUDE_API_KEY`）→ mock；`apiKey` 非空 → 真實 API。不再依賴 `kDebugMode`。

- [ ] **Step 4: 執行測試確認通過**

```powershell
flutter test test/m4b_test.dart 2>&1 | Select-Object -Last 5
```

Expected: `+8: All tests passed!`（4 AppTheme + 4 AiService）

- [ ] **Step 5: 執行全部測試**

```powershell
flutter test 2>&1 | Select-Object -Last 5
```

Expected: All passed（41 tests）

- [ ] **Step 6: dart analyze**

```powershell
dart analyze lib/ 2>&1
```

Expected: `No issues found!`

- [ ] **Step 7: Commit**

```powershell
git add lib/core/ai/ai_service.dart test/m4b_test.dart
git commit -m "feat(m4b): AiService uses real Claude API when key is provided"
```

---

## Task 6: 最終驗收 + merge to dev

- [ ] **Step 1: dart analyze**

```powershell
$env:PATH = "D:\flutter\bin;$env:PATH"
cd C:\dev\career_pilot
dart analyze lib/ 2>&1
```

Expected: `No issues found!`

- [ ] **Step 2: flutter test**

```powershell
flutter test 2>&1 | Select-Object -Last 5
```

Expected: All tests passed（41）

- [ ] **Step 3: git status 確認乾淨**

```powershell
git status
```

Expected: `nothing to commit`

- [ ] **Step 4: merge feature → dev**

```powershell
git checkout dev
git merge --no-ff feature/m4b-ui-ai -m "feat(m4b): dark mode, skeleton loader, empty state, real Claude AI"
git checkout feature/m4b-ui-ai
git log --oneline -6
```

---

## 驗收標準

- [ ] `dart analyze lib/` → No issues found
- [ ] `flutter test` → 41 passed
- [ ] 裝置切深色模式 → App 自動切換
- [ ] 職缺載入中顯示 shimmer skeleton cards
- [ ] 無職缺時顯示插圖 + 說明 + 動作按鈕
- [ ] 下拉重新整理可觸發 jobListProvider
- [ ] `--dart-define=CLAUDE_API_KEY=sk-ant-xxx` 啟動 → AI card 使用真實 API
- [ ] 未傳 key → 使用 mock（不 crash）
